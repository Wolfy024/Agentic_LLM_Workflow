"""MCP (Model Context Protocol) client integration.

Provides a JSON-RPC stdio transport layer, per-server client, and a
multi-server manager that aggregates external tool schemas into the
orchestrator's unified tool registry.
"""
